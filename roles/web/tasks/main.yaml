- name: install nginx
  ansible.builtin.dnf:
    name: nginx 
    state: present
  
- name: remove html directory
  ansible.builtin.file:
    path: /usr/share/nginx/html
    state: absent

- name: create html directory
  ansible.builtin.file:
    path: /usr/share/nginx/html
    state: directory

- name: Download web app
  ansible.builtin.get_url:
    url: https://roboshop-builds.s3.amazonaws.com/web.zip
    dest: /tmp

- name: Extract application
  ansible.builtin.unarchive:
    src: /tmp/web.zip
    dest: /usr/share/nginx/html
    remote_src: yes

# - name: copy roboshop configuration
#   ansible.builtin.copy:
#     src: roboshop.conf
#     dest: /etc/nginx/default.d/roboshop.conf    

# we are commenting the below text and re using it with templates 
# templates contain .j2 extention and a folder named templates we store our variabes there with some values

- name: copy roboshop configuration
  ansible.builtin.template:
    src: roboshop.conf.j2   # using templates
    dest: /etc/nginx/default.d/roboshop.conf
  notify:
  - restart nginx   # using handlers

# - name: start and enable nginx
#   ansible.builtin.service:
#     name: nginx
#     state: restarted
#     enabled: yes

# we are doing above operation using handlers by using above syntax

